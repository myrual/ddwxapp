<!--videointopics.wxml-->
<view class="unit" wx:for="{{opentopics}}">
            <button id="{{item.id}}" class="btn btn-primary" type="default" size="mini" bindtap="choosetopic"># {{item.title}}</button>
</view>