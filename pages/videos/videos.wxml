<!--videos.wxml-->
  <view class="unit" wx:for="{{videos}}">
          <text>{{item.title}}</text>
            <video src="{{item.video_url}}"></video>
  </view>

<view class="image-plus image-plus-nb" bindtap="addVideoInTopics">
  <view class="plusbutton">
  +</view>
</view>