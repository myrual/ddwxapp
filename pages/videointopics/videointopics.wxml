<!--videointopics.wxml-->
<button class="btn-area" id="{{item.id}}" type="primary"  size="mini" bindtap="chooseImage">我也说10秒</button>
<view class="unit" wx:for="{{opentopics}}">

<view wx:if="{{item.videourl != null}}"> 
  <video  src="{{item.videourl}}"></video>
</view>
</view>